const navLinks=document.querySelectorAll('nav a');navLinks.forEach(link=>link.addEventListener('click',e=>{e.preventDefault();document.querySelector(link.getAttribute('href')).scrollIntoView({behavior:'smooth'});}));window.addEventListener('scroll',()=>{let c='';document.querySelectorAll('section').forEach(s=>{if(scrollY>=s.offsetTop-120)c=s.id;});navLinks.forEach(l=>l.classList.toggle('active',l.getAttribute('href')=='#'+c));});document.querySelector('footer p').textContent='© '+new Date().getFullYear()+' Geetha Sri | Java Full Stack Developer';